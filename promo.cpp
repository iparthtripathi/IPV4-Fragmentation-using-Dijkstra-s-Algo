#include <bits/stdc++.h>
using namespace std;
void main(){
    int n,q;
    cin>>n>>q;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+n);
    while(q--){
        int x,y;
        cin>>x>>y;
        
    }
}